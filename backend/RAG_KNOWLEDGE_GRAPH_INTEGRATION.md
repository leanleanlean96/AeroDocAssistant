# Интеграция RAG с Графом Знаний

## Обзор

Класс `RAGModel` теперь полностью интегрирован с графом знаний Neo4j. Это позволяет:

1. **Расширять контекст** - добавлять связанные документы к ответам
2. **Обнаруживать конфликты** - предупреждать о противоречиях в документации
3. **Навигация по документам** - предлагать связанные спецификации и стандарты
4. **Семантический поиск** - использовать глоссарий для более точного поиска
5. **Анализ влияния** - показывать, какие документы зависят от изменений

## Архитектура

```
RAGModel
├── Qdrant Vector DB (полнотекстовый поиск)
├── YandexGPT LLM (генерация ответов)
├── SentenceTransformer (эмбеддинги)
└── KnowledgeGraphQueries (граф знаний)
    ├── Документы (спецификации, стандарты, чертежи)
    ├── Связи между документами
    ├── Конфликты версий
    ├── Устаревшие ссылки
    ├── Термины глоссария
    └── Аббревиатуры
```

## Использование

### 1. Создание RAGModel с графом знаний

```python
from src.services.llm.main import RAGModel

# С графом знаний (по умолчанию)
rag = RAGModel(enable_knowledge_graph=True)

# Без графа знаний (более быстро)
rag = RAGModel(enable_knowledge_graph=False)
```

### 2. Выполнение запроса

```python
question = "Какие требования к болтам?"
answer = rag.ask(question, "aviation_docs")
print(answer)
```

Система автоматически:
- Найдёт релевантные документы в Qdrant
- Обогатит контекст информацией из графа знаний
- Обнаружит конфликты и устаревшие ссылки
- Сгенерирует ответ с указанием источников

### 3. Получение связанных документов

```python
# Найти документы, связанные со SPEC-WING-001
related = rag.get_related_documents("SPEC-WING-001", max_depth=2)
for doc in related:
    print(f"{doc['doc_id']}: {doc['title']}")
```

### 4. Обнаружение конфликтов

```python
conflicts = rag.get_document_conflicts()
for conflict in conflicts:
    print(f"{conflict['doc1_id']} ↔ {conflict['doc2_id']}")
    print(f"Описание: {conflict['description']}")
    print(f"Критичность: {conflict['severity']}")
```

### 5. Поиск по терминам

```python
# Найти документы, упоминающие "лонжерон"
docs = rag.find_documents_by_term("лонжерон")

# Получить определение термина
term_def = rag.get_term_definition("лонжерон")
print(term_def['definition'])
```

## Новые возможности

### Автоматическое расширение контекста

При ответе на вопрос система автоматически:
1. Ищет документы по содержимому
2. Обнаруживает конфликты (если они релевантны)
3. Находит устаревшие ссылки
4. Добавляет всю информацию в контекст для LLM

### Результат:

```
Вопрос: Какие стандарты применяются к болтам?

Ответ:
[Полный ответ с дополнительной информацией]

📄 Связанные документы в системе:
  - SPEC-WING-001: Спецификация крепежных элементов лонжерона крыла
  - STD-045: ГОСТ Р 52644-2006. Болты высокопрочные авиационные
  - STD-189: СТО 456-2019. Моменты затяжки авиационных соединений

⚠️ Обнаруженные конфликты в документации:
  - STD-078 ↔ STD-189: Разные значения моментов затяжки для болтов М10

🔄 Устаревшие ссылки:
  - ГОСТ 1759.4-87 → ГОСТ Р 52644-2006

Источники: SPEC-WING-001, STD-045, STD-189
```

## API Reference

### Основные методы

#### `__init__(enable_knowledge_graph=True)`
Инициализация RAG модели

**Параметры:**
- `enable_knowledge_graph` (bool): Включить интеграцию графа знаний

#### `ask(user_question, collection_name)`
Задать вопрос системе

**Параметры:**
- `user_question` (str): Вопрос пользователя
- `collection_name` (str): Название коллекции Qdrant

**Возвращает:**
- str: Ответ с указанием источников

#### `get_related_documents(doc_id, max_depth=2)`
Получить связанные документы

**Параметры:**
- `doc_id` (str): ID документа
- `max_depth` (int): Максимальная глубина связей

**Возвращает:**
- list[dict]: Список связанных документов

#### `get_document_conflicts()`
Получить все конфликты в документации

**Возвращает:**
- list[dict]: Список конфликтов

#### `find_documents_by_term(term)`
Найти документы по термину

**Параметры:**
- `term` (str): Термин для поиска

**Возвращает:**
- list[dict]: Список документов

#### `get_term_definition(term)`
Получить определение термина

**Параметры:**
- `term` (str): Термин

**Возвращает:**
- dict|None: Определение и связанная информация

#### `close()`
Закрыть соединения

## Примеры использования

### Пример 1: Анализ зависимостей

```python
rag = RAGModel()

# Найти все документы, зависящие от стандарта
related = rag.get_related_documents("STD-045", max_depth=3)
print(f"Документы, зависящие от STD-045: {len(related)}")
```

### Пример 2: Проверка консистентности

```python
rag = RAGModel()

conflicts = rag.get_document_conflicts()
if any(c['severity'] == 'высокая' for c in conflicts):
    print("⚠️ Обнаружены критичные конфликты!")
    for conflict in conflicts:
        if conflict['severity'] == 'высокая':
            print(f"  {conflict['description']}")
```

### Пример 3: Просмотр информации о компоненте

```python
rag = RAGModel()

# Получить информацию о лонжероне
docs = rag.find_documents_by_term("лонжерон")
term_def = rag.get_term_definition("лонжерон")

print(f"Определение: {term_def['definition']}")
print(f"Документы, упоминающие этот термин:")
for doc in docs:
    print(f"  - {doc['title']}")
```

## Производительность

- **Поиск в Qdrant**: ~100ms для 10 результатов
- **Запрос к графу знаний**: ~50-200ms в зависимости от сложности
- **Генерация ответа LLM**: 1-5 сек в зависимости от длины

## Оптимизация

Для улучшения производительности:

1. **Кеширование результатов**:
```python
from functools import lru_cache

@lru_cache(maxsize=128)
def get_term_info(term):
    return rag.get_term_definition(term)
```

2. **Асинхронные запросы**:
```python
import asyncio

async def query_with_kg(question):
    # Параллельные запросы
    pass
```

3. **Использование batch операций**:
```python
# Запрос нескольких терминов одновременно
terms = ["лонжерон", "болт", "заклёпка"]
definitions = [rag.get_term_definition(t) for t in terms]
```

## Troubleshooting

### Ошибка: "Граф знаний недоступен"

Убедитесь, что:
1. Neo4j запущен и доступен
2. Файл `env/neo4j.env` содержит правильные учётные данные
3. Граф знаний построен: `python src/services/knowledge_graph.py`

### Ошибка: "No such file or directory"

Проверьте, что находитесь в директории `backend`:
```bash
cd backend
python examples_rag_integration.py
```

### Медленные запросы

Убедитесь, что:
1. Neo4j имеет достаточно памяти
2. Индексы созданы на графе
3. Используйте `enable_knowledge_graph=False` если граф не нужен

## Интеграция с FastAPI

```python
from fastapi import FastAPI
from src.services.llm.main import RAGModel

app = FastAPI()
rag = RAGModel()

@app.post("/ask")
async def ask_question(question: str):
    answer = rag.ask(question, "aviation_docs")
    return {"answer": answer}

@app.get("/conflicts")
async def get_conflicts():
    return rag.get_document_conflicts()

@app.get("/related/{doc_id}")
async def get_related(doc_id: str):
    return rag.get_related_documents(doc_id)
```

## Дальнейшее развитие

Планируемые улучшения:
- [ ] Асинхронные запросы к графу знаний
- [ ] Кеширование на уровне приложения
- [ ] Интеграция с Elasticsearch для полнотекстового поиска
- [ ] Визуализация графа в веб-интерфейсе
- [ ] Экспорт отчётов в PDF
- [ ] Мониторинг качества ответов
